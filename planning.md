# Provenance Guard — Planning

## Problem

Creative-sharing platforms need to tell whether posted work was written by a human
or generated by AI — not to police creativity, but to protect attribution and give
readers honest context. **Provenance Guard** is a backend any such platform can plug
in to classify submitted text, score its confidence, surface a transparency label,
and let creators appeal a classification.

**Guiding principle:** on a creative platform, a *false positive* (labelling a human's
work as AI) is worse than a false negative. Every design decision below — thresholds,
label wording, the appeals path — is tilted to avoid wrongly accusing a human.

---

## Architecture

### Submission flow

```
SUBMISSION FLOW
                         raw text            raw text
  client ──POST /submit──▶ app.py ──┬──────────────────▶ llm_signal (Groq)
                             │       │                      │ {p_ai, rationale}
                             │       └──────────────────▶ stylometry_signal
                             │                              │ {p_ai, features}
                             │        signal scores ────────┘
                             ▼
                          scoring.py ── p_ai + confidence ──▶ labels.py
                             │                                   │ label text
                             ├── audit entry ──▶ audit_log.jsonl │
                             ├── record ──▶ store (submissions.json)
                             ▼                                   │
  client ◀──── JSON: {id, verdict, confidence, label, signals} ◀┘

APPEAL FLOW
  client ──POST /appeal──▶ app.py ──▶ store: status = "under review"
   {submission_id,               └──▶ audit_log.jsonl (appeal + orig decision)
    reason}          ◀── JSON: {appeal_id, status:"under review"} ──┘
```

### Narrative — the path one text takes

1. **`POST /submit`** receives raw text (and an optional `creator_id`). `app.py`
   validates it (non-empty, under the size cap).
2. The text is fanned out to **two independent signals**:
   - **`llm_signal`** (Groq `llama-3.3-70b-versatile`) returns a probability the text
     is AI-written plus a one-line rationale.
   - **`stylometry_signal`** (pure Python) computes structural features and returns
     its own AI-probability.
3. **`scoring.combine`** merges the two into a single `p_ai` and a `confidence`,
   applying an *agreement check* (disagreeing signals lower confidence) and
   *false-positive-aware thresholds* (calling something AI takes a higher bar than
   calling it human).
4. **`labels.build_label`** maps the verdict + confidence to one of **three**
   plain-language transparency labels.
5. The full decision (verdict, confidence, both signal scores, timestamp) is appended
   to the **audit log** (`audit.py` → `audit_log.jsonl`) and the submission is saved
   (`store.py` → `submissions.json`).
6. A structured JSON response goes back to the client.

**Appeal flow:** `POST /appeal` takes a `submission_id` + the creator's reasoning →
the stored submission's status flips to `"under review"` → the appeal (reason + a
snapshot of the original decision) is appended to the audit log → a confirmation
with the appeal id and new status is returned. No automated re-classification — a
human reviews contested content.

### Components

| File | Responsibility |
|------|----------------|
| `app.py` | Flask routes, request validation, rate-limit wiring, response shaping |
| `config.py` | Single source of truth: model, thresholds, weights, rate limits, paths |
| `detection/llm_signal.py` | Signal 1 — Groq classifier → `{p_ai, rationale}` |
| `detection/stylometry_signal.py` | Signal 2 — structural heuristics → `{p_ai, features}` |
| `detection/scoring.py` | Combine signals → verdict + confidence |
| `detection/labels.py` | The three transparency-label variants |
| `store.py` | JSON persistence for submissions & appeals (lock-guarded) |
| `audit.py` | Append-only JSONL audit log + reader |

---

## Detection signals

The system uses **two genuinely distinct signals** — one *semantic*, one *structural*
— so their combination is more informative than either alone.

### Signal 1 — LLM classifier (Groq `llama-3.3-70b-versatile`)

- **What it measures:** holistic semantic/stylistic coherence — does the passage
  *read* as machine-generated? The model is asked for a calibrated 0–1 AI-probability
  plus a one-line rationale, returned as strict JSON.
- **Why it differs between human and AI:** AI prose tends to be fluent-but-generic,
  evenly hedged, and low in idiosyncrasy — patterns a strong model recognises.
- **Blind spot:** nondeterministic and can be overconfident; lightly-edited AI or an
  unusual-but-human voice can fool it. On any API or parse failure it falls back to
  `p_ai = 0.5` ("can't tell") rather than guessing, so the pipeline stays honest.

### Signal 2 — Stylometric heuristics (pure Python)

Structural properties that are independent of meaning:

- **Burstiness (sentence-length variance):** humans mix long and short sentences; AI
  is more uniform in rhythm. Measured as the coefficient of variation of sentence
  lengths.
- **Type-token ratio (vocabulary diversity):** how often words are reused. AI often
  leans on a narrower, safer vocabulary.
- **Punctuation density:** humans use varied marks (dashes, semicolons, parentheses);
  AI trends toward plainer comma-and-period prose.

Each feature is normalised to a 0–1 "AI-likeness" and averaged into the signal's `p_ai`.

- **Why it differs:** these are statistical fingerprints the LLM's semantic read can
  miss — a structural cross-check.
- **Blind spot:** short texts are noisy (few sentences → unstable variance), and a
  formulaic human or a deliberately varied AI can invert the pattern. Below 4
  sentences the signal is blended back toward 0.5, and it never decides alone.

---

## Confidence scoring (design-first)

We decided what the numbers should **mean** before choosing the math.

- `p_ai` — combined probability the text is AI (0–1).
- `confidence` — how sure we are of the verdict. **0.5-ish `p_ai` → ~0 confidence;
  the extremes → ~1.** This is the number shown to users.

**Combine:** `p_ai = 0.65 · llm_p_ai + 0.35 · stylometry_p_ai`. The LLM read is
weighted higher because it captures meaning the stylometry is blind to; stylometry is
the sanity check.

**Agreement adjustment:** if the two signals land on opposite sides of 0.5 they
disagree — we pull `p_ai` halfway back toward 0.5 and cap `confidence`, so conflict
surfaces as doubt rather than a confident-but-fragile verdict.

**Verdict thresholds (false-positive asymmetry):**

| Condition | Verdict | Label variant |
|-----------|---------|---------------|
| `p_ai ≥ 0.80` **and** signals agree | `likely_ai` | high-confidence AI |
| `p_ai ≤ 0.25` | `likely_human` | high-confidence human |
| otherwise (incl. a 0.51) | `uncertain` | uncertain |

The "AI" zone is deliberately narrow: declaring AI needs strong, *agreeing* evidence,
while the default for anything ambiguous is `uncertain` — never a stray accusation.

---

## The false-positive scenario (traced through the system)

A human poet submits a spare, tightly-controlled poem. Its short, uniform lines make
the **stylometry** signal read AI-like (low burstiness, modest TTR). But:

1. The **LLM** signal reads it as human (`p_ai ≈ 0.2`), so the two signals **disagree**.
2. `scoring.combine` detects the disagreement, pulls the combined `p_ai` toward 0.5,
   and caps confidence.
3. The combined `p_ai` never clears the 0.80 AI bar, so the verdict is **`uncertain`**,
   not `likely_ai`.
4. The reader sees the **uncertain** label ("we're not making a call"), not an
   accusation.
5. If the creator still disputes it, **`POST /appeal`** logs their reasoning, flips the
   status to `under review`, and records the appeal beside the original decision in the
   audit log for a human to examine.

The disagreement mechanism plus the narrow AI zone plus the appeal path are three
independent guards against the costly error.

---

## API surface (the contract)

| Endpoint | Request | Response | Notes |
|----------|---------|----------|-------|
| `POST /submit` | `{ "content": str, "creator_id"?: str }` | `{ submission_id, verdict, confidence, p_ai, label:{variant,text}, signals:{llm,stylometry}, status }` | Rate-limited; 400 on empty/oversized content |
| `POST /appeal` | `{ "submission_id": str, "reason": str }` | `{ appeal_id, submission_id, status:"under review", message }` | 404 unknown id, 400 empty reason |
| `GET /log` | — | `[ audit entries… ]` (oldest first) | Structured audit log |
| `GET /health` | — | `{ "status": "ok" }` | Liveness |

---

## Rate limiting

Flask-Limiter, per client IP:

- **`POST /submit`: 10/minute and 100/day.** A real creator submits a handful of pieces
  a day and may retry a few times in a burst, so 10/min covers legitimate use without
  friction; 100/day sits far above human use but throttles an adversary flooding the
  classifier (each call is a paid Groq request).
- **`POST /appeal`: 20/hour** — appeals are rare; this stops abuse while never blocking
  a genuine dispute.
- **`GET /log`: unlimited** — read-only.

---

## Audit log

Append-only JSONL, one object per event.

- **decision:** `{ event, submission_id, timestamp, creator_id, verdict, confidence, p_ai, signals:{llm, stylometry} }`
- **appeal:** `{ event, appeal_id, submission_id, timestamp, reason, original_decision:{verdict, confidence, p_ai} }`

Surfaced via `GET /log`.

---

## Decisions log

- **Storage = JSON files**, not a DB — matches project scope; `store.py` uses a lock +
  atomic temp-file swap so concurrent requests don't clobber writes.
- **Two signals only** (required-features build; no stretch features).
- **LLM failure is non-fatal** — degrades to `p_ai = 0.5` so the service never invents a
  verdict when Groq is unavailable.
- **Uncertain is the default verdict** — the system says "I don't know" freely.

---

## Scope

**In scope (required):** submission endpoint, 2-signal pipeline, confidence scoring with
uncertainty, three transparency labels, appeals workflow, rate limiting, structured audit
log. **Out of scope:** ensemble (3+ signals), provenance certificate, analytics dashboard,
multi-modal — the four stretch features are intentionally not built.
